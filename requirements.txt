# ====================================================================
# Amaro Aviation Calculator v3.0 REFATORADO - Dependências Simplificadas
# Sistema consolidado com fallbacks automáticos e foco na UX
# ====================================================================

# === FRAMEWORK PRINCIPAL ===
streamlit>=1.28.0,<2.0.0
# Framework web principal - interface moderna e responsiva

# === PROCESSAMENTO DE DADOS ===
pandas>=2.0.0,<3.0.0
# Manipulação de dados, CSVs e DataFrames

numpy>=1.24.0,<2.0.0
# Computação numérica essencial

# === VISUALIZAÇÃO PREMIUM ===
plotly>=5.15.0,<6.0.0
# Gráficos interativos de alta qualidade

# === EXPORTAÇÃO ROBUSTA ===
openpyxl>=3.1.0,<4.0.0
# Exportação Excel (formato xlsx) - principal

xlsxwriter>=3.1.0,<4.0.0
# Exportação Excel com formatação avançada - fallback

reportlab>=4.0.0,<5.0.0
# Geração de PDFs profissionais

# === UTILITÁRIOS ===
Pillow>=10.0.0,<11.0.0
# Processamento de imagens (logos, gráficos)

python-dateutil>=2.8.0,<3.0.0
# Manipulação de datas e timestamps

# ====================================================================
# OBSERVAÇÕES IMPORTANTES PARA A VERSÃO REFATORADA:
#
# 1. ARQUITETURA SIMPLIFICADA:
#    - App.py consolidado com 3 abas principais
#    - Removidas dependências desnecessárias
#    - Foco em performance e estabilidade
#
# 2. FALLBACKS AUTOMÁTICOS:
#    - Excel (openpyxl) → Excel (xlsxwriter) → CSV → JSON
#    - PDF (reportlab) → TXT → HTML
#    - Sistema robusto que sempre funciona
#
# 3. COMPATIBILIDADE:
#    - Python 3.8+ (testado até 3.11)
#    - Streamlit Cloud ready
#    - Deploy local e cloud
#
# 4. INSTALAÇÃO SIMPLIFICADA:
#    pip install -r requirements.txt
#    python setup_initial.py
#    streamlit run app.py
#
# 5. VERSÕES TESTADAS (Outubro 2024):
#    - streamlit==1.29.0
#    - pandas==2.1.3
#    - plotly==5.17.0
#    - openpyxl==3.1.2
#
# ====================================================================